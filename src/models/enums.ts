export enum Status {
    submitted = 'Submitted',
    'home assignment' = 'Home Assignment',
    'scheduled interview' = 'Scheduled Interview',
    contract = 'Contract',
    rejection = 'Rejection'
}

export enum ElementType {
    textInput = 'text',
    textarea = 'textarea',
    numInput = 'number',
    rangeInput = 'range'
}