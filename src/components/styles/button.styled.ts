import styled from "styled-components"

export const StyledButton = styled.button`
   padding: 1em;
   border-radius: 3px;
   border: none;
   background-color: #fff;
   &:hover{
    cursor: pointer;
   }
`